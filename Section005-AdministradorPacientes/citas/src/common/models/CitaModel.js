class CitaModel {
    constructor(mascota, propietario, hora, fecha, sintomas) {
        this.mascota = mascota;
        this.propietario = propietario;
        this.fecha = fecha;
        this.hora = hora;
        this.sintomas = sintomas;
    }
    
}

export default CitaModel;